"""RugGuard MCP server — exposes scan / explain to MCP-compatible agents.

Run `python -m rugguard_mcp` to launch the server over stdio (the default
transport for Claude Desktop, Cursor, and other MCP clients). Configuration
is via env vars; see server.py for the full list.
"""
